# Roadmap

## Condition System to filter documents

Add support for more complex conditions (e.g., nested conditions, logical operators). which we have somewhere (`laife/src/laife/llm_services/vectorstores/cond.py`)

analyze existing,
check how to make them more flexible and composable, and then implement the new design in `llm_core` and migrate the existing code to use it.
